import React from 'react';
import "./fallback.css"

const Fallback = () => {
    return (
        <div className="fallback">
            <h3 className="fallback-h3">
                Loading...
            </h3>
        </div>
    )
}

export default Fallback
